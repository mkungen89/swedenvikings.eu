# ===========================================
# Docker Ignore - Sweden Vikings CMS
# ===========================================

# Dependencies
node_modules
**/node_modules

# Build outputs (we build fresh in Docker)
dist
**/dist
client/dist
server/dist

# Environment files (use .env.production on server)
.env
.env.*
!.env.example

# Git
.git
.gitignore

# IDE
.vscode
.idea
*.swp
*.swo

# Logs
*.log
logs

# OS files
.DS_Store
Thumbs.db

# Testing
coverage
.nyc_output

# Documentation
*.md
!README.md

# Docker files (no need to copy these into image)
Dockerfile*
docker-compose*.yml
.dockerignore

# Scripts (only needed on host)
scripts

# Nginx config (used on host, not in app container)
nginx

# Development files
.eslintrc*
.prettierrc*
tsconfig*.json
jest.config.*

# Uploads (mounted as volume)
uploads

# Temporary files
tmp
temp
*.tmp
