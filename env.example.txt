# ============================================
# Sweden Vikings CMS - Environment Variables
# ============================================
# Rename this file to .env and fill in your values

# App Configuration
NODE_ENV=development
PORT=3001
CLIENT_URL=http://localhost:5173

# Database (PostgreSQL via Docker)
DATABASE_URL="postgresql://swedenvikings:vikings2024@localhost:5432/swedenvikings"

# Redis (via Docker)
REDIS_URL="redis://localhost:6379"

# Session
SESSION_SECRET=sweden_vikings_super_secret_session_key_2024_change_in_production

# Steam Authentication
# Get your API key at: https://steamcommunity.com/dev/apikey
STEAM_API_KEY=FAFAAC74BFF2F72980D8E53E1A2B4A69
STEAM_REALM=http://localhost:3001
STEAM_RETURN_URL=http://localhost:3001/api/auth/steam/callback

# Discord Webhooks (optional)
DISCORD_WEBHOOK_NEWS=
DISCORD_WEBHOOK_EVENTS=
DISCORD_WEBHOOK_SERVER=
DISCORD_WEBHOOK_ADMIN=

# Email (Mailhog for development)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@swedenvikings.eu

# Arma Reforger Server
ARMA_SERVER_PATH=C:/ArmaReforgerServer
ARMA_SERVER_EXE=ArmaReforgerServer.exe
STEAMCMD_PATH=C:/SteamCMD

# File Uploads
UPLOAD_MAX_SIZE=5242880
UPLOAD_PATH=./uploads

# Security
CORS_ORIGIN=http://localhost:5173
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# Backup
BACKUP_PATH=./backups
BACKUP_RETENTION_DAYS=30

